# Remaining exceptions after Well-Architected hardening.
# Raw Checkov still uses the inline #checkov:skip comments in *.tf.
compact: true
framework:
  - terraform
skip-check:
  - CKV_AWS_382
soft-fail: false
